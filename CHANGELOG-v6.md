# 不不tarot V6 — 自有内容工作台 + 78 张牌扇

## 这次改了什么

### 1. 后台升级为“页面文案工作台”
- 按页面切换：全站通用、首页、梦向解读、现实问题咨询、奇思妙想、预约页、小科普、联系前确认、政策页面。
- 目前登记了 284 个可编辑文案字段，包含大标题、副标题、正文、按钮、卡片文字、页脚、状态提示和政策页面外壳。
- 支持搜索字段，适合直接搜“标题”“按钮”或当前文案中的词。
- 每个页面都有“打开当前页面”入口，保存后可快速检查效果。
- 原有价格、活动、联系方式、政策正文、排版设置和小科普卡片仍继续保留。

### 2. 批量 JSON 编辑
- 当前页面可以一键导出 JSON。
- 可以把 JSON 复制到 ChatGPT / 文本编辑器里批量改，再整段粘回后台。
- 导入时只接受当前页面已经登记的字段，多余字段会忽略，不会执行代码。

### 3. 自由内容块
- 可在页面主视觉之后或页脚之前插入自定义内容。
- 支持 Markdown / 普通文本。
- 支持受限安全 HTML（常见标题、段落、列表、链接、引用、代码块等）。
- 不执行 `<script>`、iframe、onclick 等任意 JavaScript，避免存储型 XSS 影响后台登录态和网站安全。
- 每个内容块可选择显示页面、位置、模式，也可随时隐藏或删除。

### 4. 小科普改为 78 张塔罗牌扇
- 在一个固定展示框中渲染 78 张背面牌。
- 牌按半椭圆弧排列，用户可以点击任意一张。
- 抽中的牌会离开牌扇并翻面，再显示随机小科普。
- 78 张动画牌不与科普条目一一绑定；增删科普卡片无需维护牌面映射。
- 桌面和手机端都有独立尺寸适配。

### 5. 以前写死的页面文字继续解硬编码
- 奇思妙想页、预约页、小科普页、联系确认弹窗、政策总页的大部分可见文字已经进入内容工作台。
- 首页、梦向、现实咨询原有后台字段继续保留并重新整理。
- 文案字段允许保存空字符串，不会因为清空而自动恢复默认句子。

## 数据兼容
内容版本从 V7 升级为 V8。读取旧 `site-content.json` 时：
- 原价格保留；
- 原活动保留；
- 原联系方式和预约政策保留；
- 原页面文案保留；
- 新增字段自动补默认值；
- `richBlocks` 默认空数组，不会改变现有页面布局。

更新脚本会在重建前把当前 `vps/data/site-content.json` 备份到 `vps/data/backups/site-content-before-v6-时间戳.json`。

## VPS 更新
覆盖服务器上的项目代码后运行：

```bash
chmod +x vps/update-existing-server.sh
./vps/update-existing-server.sh
```

也可以继续使用：

```bash
docker compose -f vps/docker-compose.yml build
docker compose -f vps/docker-compose.yml up -d
```

## 本地检查结果
- 43 个 TypeScript / TSX 源文件全部通过 TypeScript transpile 语法检查。
- 284 个后台文案字段均能在默认内容中找到对应键。
- 使用旧 V7 内容做清洗测试时，原有价格和自定义首页标题均得到保留，新字段自动补齐。
- 当前执行环境没有 Docker，且外网不可访问 npm registry，因此未在此环境完成最终 `next build`；服务器 Docker 构建仍会执行正式 Next.js 编译。
