# V10.2 FinalShell 更新说明

这份包可以直接覆盖当前 `/root/bubu-tarot-site`。

如果之前 V10 构建停在 `components/KnowledgeDeck.tsx` 的 TypeScript 报错，也不用手动删文件：V10.2 的更新脚本会自动清理这份已退役源码。

## 1. 上传

把：

`bubu-tarot-vps-v10.2-spread-sections.tar.gz`

拖到 FinalShell 的 `/root/`。

## 2. 校验

```bash
ls -lh /root/bubu-tarot-vps-v10.2-spread-sections.tar.gz
sha256sum /root/bubu-tarot-vps-v10.2-spread-sections.tar.gz
```

请与 ChatGPT 提供的 SHA-256 对照。

## 3. 备份当前网站

```bash
tar -C /root -czf "/root/bubu-tarot-site-before-v10.2-$(date +%Y%m%d_%H%M%S).tar.gz" bubu-tarot-site
```

## 4. 检查配置和数据

```bash
test -f /root/bubu-tarot-site/vps/.env && echo "1. .env 存在：OK"
test -d /root/bubu-tarot-site/vps/data && echo "2. 数据目录存在：OK"
grep '^PUBLIC_ORIGIN=' /root/bubu-tarot-site/vps/.env
```

## 5. 覆盖代码

```bash
tar -xzf /root/bubu-tarot-vps-v10.2-spread-sections.tar.gz -C /root/bubu-tarot-site
```

## 6. 覆盖后检查

```bash
cd /root/bubu-tarot-site
test -f vps/.env && test -d vps/data && test -f vps/update-existing-server.sh && echo "覆盖后检查：OK"
```

## 7. 正式更新

```bash
chmod +x vps/update-existing-server.sh
./vps/update-existing-server.sh
```

最终应看到：

`Update complete.`

如果出现 `Type error`、`failed`、`ERROR`、`unhealthy`，或没有出现 `Update complete.`，先不要运行清理命令，把报错截图/文本发给 ChatGPT。

## 8. 浏览器验收

打开 `/notes`：

- 牌阵小册顶部应该出现「塔罗牌阵 / 雷诺曼牌阵」。
- 塔罗区应显示原来的 6 个牌阵。
- 雷诺曼区应显示 4 个牌阵。
- 3×3 与 Grand Tableau 的示意图应正常排列。

进入后台：

- 11 / SPREAD NOTES 中，每个牌阵应出现“所属体系”。
- 新增牌阵时可以自己选择塔罗或雷诺曼。
