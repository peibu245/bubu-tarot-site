# V10.4 审核报告

## 范围

V10.4 只修改抽牌视觉层与雷诺曼静态牌面资源：Dondorf 正面、极简牌背延续、牌扇尺寸/弧度、抽出动画归正。未修改价格、联系方式、服务条款、后台保存数据结构或牌阵内容。

## 已完成检查

### 雷诺曼资产
- 36/36 张存在。
- 全部为 JPEG，可正常解码。
- 全部尺寸 477×777。
- 36 个 SHA-256 均不同，无重复文件。
- 从 V10 的未改动 Dondorf 公版裁切重新生成，不继承 V10.3 的旧展示图。
- 新路径为 `public/tarot/lenormand/dondorf-v104/`。

### 抽牌逻辑
- 韦特可见窗口：桌面 27、手机 15；实际抽取仍为完整 78 张。
- 雷诺曼可见窗口：桌面 23、手机 13；实际抽取仍为完整 36 张。
- 无放回 shuffle-bag 逻辑未改。
- 抽出动画不再继承扇面 `rotation`；移动阶段固定 0deg。
- 点击网格同步调整为桌面 11 / 手机 7 个较宽触控区。

### 源码静态检查
- 按 Dockerfile overlay 方式生成生产源码树。
- 生产 app/components/lib 共 39 个 TS/TSX 文件。
- 相对 import 缺失：0。
- TypeScript `transpileModule` 语法诊断：0。
- `vps/update-existing-server.sh` 与 `vps/setup-server.sh` 通过 `bash -n`。
- `scripts/fetch-public-card-assets.py` 通过 `py_compile`。

### 部署安全
- 更新脚本继续先备份 `site-content.json`。
- V10.4 会清理旧 `dondorf` 与 `dondorf-v103` 静态目录。
- `.env` 与 `vps/data` 不由发布包覆盖。
- Docker build 失败时不会执行成功替换流程；仍以最终 `Update complete.` 为上线成功标准。

## 尚未在本地完成的一层

尝试在当前沙箱按 VPS `package.json` 安装 Next.js 依赖以执行完整 `next build --webpack`，但 `npm install` 在当前运行环境长时间无进展后终止。因此没有声称本地 production build 已完成。真正的 Next.js 类型检查/production build 仍由 VPS Docker build 执行；构建失败不会得到 `Update complete.`。
