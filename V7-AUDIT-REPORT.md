# 不不tarot V7 审核报告

审核日期：2026-08-19

## 本轮已修复

1. 历史版本由文件名排序改为真实修改时间排序，避免部署前备份误显示成最近版本。
2. 除首次保存不存在源文件的正常 ENOENT 外，备份失败会阻止发布，避免“保存成功但回滚保护其实没生成”。
3. 内容写入使用唯一临时文件名，降低写入冲突风险。
4. 主内容 JSON 损坏/异常时优先读取最近有效历史备份；除真正首次运行外，不再悄悄展示默认价格/默认文案。
5. 历史清理仅处理 `site-content-*.json`，不会误删备份目录中的其他 JSON。
6. 历史版本按修改时间保留最近 30 份。
7. 模块拖拽上下方向的插入逻辑统一。
8. 保存或回滚后实时预览会刷新，避免价格、模块等仍显示旧状态。
9. 动态联系须知弹窗采用增量映射，不会因为弹窗出现而丢失页面上已有的草稿文字映射。
10. 预约页接入全站排版设置并补充可编辑文案标记。
11. 更新脚本保留现有有效 `PUBLIC_ORIGIN`，不再无条件改回生产域名。
12. 更新脚本在 build 前验证管理员密码、session secret、PUBLIC_ORIGIN。
13. `.env` 缺少关键键时会给出明确错误，不会因 `set -e` 提前无提示退出。
14. 移除发布包中的 TypeScript build-info 临时文件。

## 已通过的检查

- TypeScript/TSX 静态转译语法检查：37 个文件，0 个语法诊断。
- Docker overlay 模拟后的相对 import 检查：33 个源码文件，0 个缺失相对依赖。
- 页面文案 schema：284 个字段，284 个唯一键，0 重复，0 个默认值缺失。
- 所有 shell 脚本 `bash -n`：通过。
- 内容保存、备份、排序、恢复、恢复前再备份：真实文件读写测试通过。
- 主内容损坏时从最新有效历史备份恢复：通过。
- V6 旧自由内容块迁移为 V7 富文本模块：通过。
- 连续保存后的历史版本上限 30：通过。
- 更新脚本正常服务器模拟：保留既有域名/密码/secret、先备份内容、调用 build/up、健康检查通过。
- 更新脚本错误配置模拟：在 build 前明确停止并提示缺少 ADMIN_PASSWORD。
- 发布包检查：不包含真实 `.env`、不包含 `vps/data`、不包含 `node_modules`、不包含 `*.tsbuildinfo`。

## 无法在当前沙箱完成的一项

当前运行环境无法可靠访问 npm registry，同时没有 Docker daemon，因此这里不能完成真正的 `next build --webpack` / Docker production build。服务器执行 `./vps/update-existing-server.sh` 时的 Docker build 是最后一道真实编译门槛。部署指南因此安排了：部署前整站备份、脚本构建前配置校验、容器 healthcheck 和部署后本机 HTTP 检查。

这意味着本轮能显著降低部署风险，但任何真实软件都不能诚实承诺“绝对不存在任何 bug”。
