# V10.4 FinalShell 部署

目标文件：`bubu-tarot-vps-v10.4-dondorf-fan.tar.gz`

1. 上传到 `/root/`，先校验 SHA-256（以发布消息中的哈希为准）。
2. 备份当前网站：
   `tar -C /root -czf "/root/bubu-tarot-site-before-v10.4-$(date +%Y%m%d_%H%M%S).tar.gz" bubu-tarot-site`
3. 确认 `/root/bubu-tarot-site/vps/.env` 与 `/root/bubu-tarot-site/vps/data` 存在。
4. 覆盖：
   `tar -xzf /root/bubu-tarot-vps-v10.4-dondorf-fan.tar.gz -C /root/bubu-tarot-site`
5. 执行：
   `cd /root/bubu-tarot-site && chmod +x vps/update-existing-server.sh && ./vps/update-existing-server.sh`
6. 只有最终出现 `Update complete.` 才视为成功。

上线后优先检查：
- 雷诺曼抽出正面是否来自 `/tarot/lenormand/dondorf-v104/`；
- 雷诺曼牌背是否淡紫 + 小圆形；
- 韦特牌背是否灰紫 + 小五角星；
- 牌扇单张是否明显变大，弧度是否比 V10.3 更自然；
- 点击任意左右倾斜牌后，抽出的牌是否从移动开始就保持竖直。
