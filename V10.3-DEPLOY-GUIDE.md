# V10.3 FinalShell 更新说明

适用于已经运行 V10.2 的 `/root/bubu-tarot-site`。

## 1. 上传并校验

把 `bubu-tarot-vps-v10.3-dondorf-backs.tar.gz` 上传到 `/root/`，然后：

```bash
ls -lh /root/bubu-tarot-vps-v10.3-dondorf-backs.tar.gz
sha256sum /root/bubu-tarot-vps-v10.3-dondorf-backs.tar.gz
```

请与发布说明中的 SHA-256 完全一致后再继续。

## 2. 备份当前站点

```bash
tar -C /root -czf "/root/bubu-tarot-site-before-v10.3-$(date +%Y%m%d_%H%M%S).tar.gz" bubu-tarot-site
ls -lh /root/bubu-tarot-site-before-v10.3-*.tar.gz
```

## 3. 确认持久化配置仍在

```bash
test -f /root/bubu-tarot-site/vps/.env && echo ".env：OK"
test -d /root/bubu-tarot-site/vps/data && echo "data：OK"
grep '^PUBLIC_ORIGIN=' /root/bubu-tarot-site/vps/.env
```

## 4. 覆盖源码

```bash
tar -xzf /root/bubu-tarot-vps-v10.3-dondorf-backs.tar.gz -C /root/bubu-tarot-site
```

## 5. 更新

```bash
cd /root/bubu-tarot-site
chmod +x vps/update-existing-server.sh
./vps/update-existing-server.sh
```

最终应看到：

```text
Image vps-bubu-tarot Built
Container bubu-tarot Started
Update complete.
```

如果出现 Type error、failed、unhealthy 或没有出现 `Update complete.`，不要删除数据或反复重装，保留现场并把错误段落发回 ChatGPT。

## 6. 浏览器验收

重点检查 `/notes`：

- 韦特牌背：纯灰紫 + 小五角星；
- 雷诺曼牌背：淡紫 + 小圆；
- 雷诺曼 01 骑士：右上不再是 Dondorf 厂牌章，而是第二枚牌号；
- 36 张雷诺曼抽取后都能正常显示；
- 牌名仍在牌面图片下方，不遮挡画面；
- “你知道吗？”遮罩、Tips、牌阵分区保持正常。

如浏览器仍显示旧雷诺曼图片，先强制刷新（Windows: `Ctrl+F5`）；V10.3 已使用新的静态资源目录，正常情况下不会继续命中旧图。
