# 不不tarot V10.2 审核报告

## 本次修改

- 牌阵小册拆分为「塔罗牌阵 / 雷诺曼牌阵」两个一级分区。
- 后台每个牌阵新增“所属体系”字段，可选塔罗或雷诺曼。
- 塔罗区保留原有 6 个牌阵。
- 雷诺曼区新增 4 个入门/进阶结构：三张牌串、五张牌串、3×3 九宫格、Grand Tableau（4×9 示意）。
- 新增 line5 / grid9 / grandtableau36 三种示意图布局。
- 牌阵小册的分区按钮文字也进入后台文案工作台。
- 更新脚本会自动删除已经退役的 `components/KnowledgeDeck.tsx`，避免原地覆盖升级后旧文件残留再次触发 TypeScript 构建错误。

## 兼容性与迁移验证

用 V10 风格旧数据实际跑过迁移：

- contentVersion 12 → 13：通过。
- 自定义首页标题：保留。
- 自定义价格：保留。
- 原 6 个牌阵自动归入塔罗区：通过。
- 新增 4 个雷诺曼牌阵：通过。
- 第二次读取不会重复添加：通过。
- 升级后手动删除某个雷诺曼牌阵，不会在下次读取时自动复活：通过。
- 旧版已经删掉的塔罗默认牌阵，不会因为升级被重新塞回来：通过。

## 代码检查

- TS / TSX 文件：54 个。
- 语法诊断：0。
- 关键改动的 TypeScript 语义检查：通过。
- VPS overlay 后 app/components/lib：39 个 TS/TSX 文件，相对 import 缺失 0。
- `update-existing-server.sh`：`bash -n` 通过。
- `setup-server.sh`：`bash -n` 通过。
- 公版牌图下载脚本：`py_compile` 通过。
- 默认牌阵：10 个，ID 全部唯一。
  - 塔罗：6
  - 雷诺曼：4
- 后台可编辑文案字段：331 个，重复 0，默认值缺失 0。

## 雷诺曼牌阵说明

当前 V10.2 采用：

1. 三张牌串：强调 1+2、2+3，再合成一句话。
2. 五张牌串：中心牌优先，再向两边展开。
3. 3×3 九宫格：中心 → 邻牌 → 横竖斜线 → 四角。
4. Grand Tableau：用常见 4×9 结构做示意，并明确提示也存在 8×4+4 等排法。

“两张组合”没有硬塞进牌阵区，后续更适合放在雷诺曼组合语法/小实验模块里。

## 尚未在本环境完成的检查

当前环境无法稳定从 npm registry 完整安装 Next.js 依赖，因此没有在这里完成真正的 `next build --webpack`。最终生产构建仍由你的 RackNerd VPS 在执行更新脚本时完成。

更新脚本只有在 Docker build 成功、容器启动并通过 healthcheck 后才会显示 `Update complete.`。
