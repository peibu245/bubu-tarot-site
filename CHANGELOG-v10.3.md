# 不不 tarot V10.3 — Dondorf + 极简牌背

## 这一版只做已经定稿的视觉修复

V10.3 不新增新的业务模块，重点把 V10.2 中漏落地的两项真正做进前台：

1. 雷诺曼牌面改为更干净的传统 B. Dondorf 视觉呈现；
2. 塔罗 / 雷诺曼使用两套极简纯色牌背。

## 雷诺曼牌面

- 仍以英国博物馆藏、Wikimedia Commons 公版机械扫描中的 19 世纪 B. Dondorf 历史图像为底图。
- 36 张全部重新输出为独立 `477 × 777` JPEG。
- 不使用 AI 重绘人物、景物、扑克嵌图或色彩内容。
- 为贴近网站选定的经典 Dondorf 展示方式，右上出版者圆章被替换成同一张牌左上牌号圆章的副本。
- 只做普通图像处理：对称版面整理、Lanczos 放大、轻微对比度与锐化。
- 使用新的 `dondorf-v103` 静态资源目录，避免浏览器继续命中 V10.2 的旧图片缓存。

## 新牌背

### 韦特–史密斯

- 纯灰紫色背景；
- 中央一个小五角星 `☆`；
- 极细内边框；
- 不使用渐变、纹样或大型神秘学装饰。

### 雷诺曼

- 更浅的淡紫色背景；
- 中央一个小圆 `○`；
- 极细内边框；
- 与塔罗保持同一设计语言但可一眼区分。

牌堆选择器中的缩略牌背也同步使用这套设计。

## 数据与部署

- 内容数据版本从 13 升到 14。
- 用户自己编辑过的文案不会被覆盖。
- 如果“雷诺曼图源说明”仍是 V10.2 默认值，则自动更新为 V10.3 的透明说明；用户自行改过的版本保持不动。
- 更新前自动备份 `site-content.json`，文件名前缀改为 `before-v10.3`。
- `KnowledgeDeck.tsx` 旧文件清理逻辑继续保留。
