# 不不tarot V10.4 — Dondorf 正面与抽牌视觉修正版

本版只处理抽牌视觉，不改价格、后台内容、联系方式、Tips 或牌阵数据。

## 1. 雷诺曼正面真正换新

- 继续使用明确公版的 19 世纪 B. Dondorf / Mlle. Lenormand 历史扫描作为底图。
- 36/36 张重新从未改动的历史裁切版生成，而不是沿用 V10.3 的旧展示图片。
- 普通非生成式图像处理：去除原本两个圆章区域、轻微修复背景，再绘制浅米色双牌号圆章；中间扑克牌嵌图与下方历史插画保持原画。
- 轻微对比度/色彩/锐度整理，Lanczos 放大到统一 477×777。
- 新资源目录：`/tarot/lenormand/dondorf-v104/`，用于绕过浏览器旧缓存。

## 2. 牌背定稿继续保留

- 韦特：纯灰紫 + 中央小五角星 + 极细内边框。
- 雷诺曼：淡紫 + 中央小圆形 + 极细内边框。
- 顶部牌组选择器的小牌堆同步使用同一视觉语言。

## 3. 牌扇放大并重画弧度

- 不再为了展示“很多张”而把每张牌压得过小。
- 桌面端：韦特视觉窗口约 27 张，雷诺曼约 23 张。
- 手机端：韦特约 15 张，雷诺曼约 13 张。
- 背后抽取逻辑仍然使用完整 78/36 张无放回洗牌袋；可见张数只是视觉窗口。
- 单张牌背明显放大，中央抬高、两侧下沉，弧度更明显，两侧自然被容器裁切。

## 4. 抽出的牌不再歪着飞

- 点击扇面后，抽出动画只继承点击位置，不再继承扇面旋转角。
- 抽出的牌从起点起即保持竖直，移动到中央后再翻面。
- 正面牌名仍位于图片下方，不覆盖历史牌面。

## 5. 部署兼容

- 更新脚本会删除旧 `dondorf` 和 `dondorf-v103` 目录，避免新旧资源混用。
- 保留现有 `.env` 与 `vps/data/site-content.json`。
- 在 Docker build 成功之前不会替换线上容器。
