# V10.3 审核报告

## 范围

V10.3 是 V10.2 的窄范围视觉修正版，只涉及：

- Dondorf 雷诺曼静态图像；
- 抽牌牌背 CSS；
- 雷诺曼静态资源路径；
- 对应的图源说明和数据版本迁移；
- 更新脚本备份版本标记。

## 图像检查

- Dondorf 文件数量：36/36
- 编号：`01.jpg` 至 `36.jpg`
- 尺寸：全部 `477 × 777`
- 格式：全部 JPEG，可由 Pillow 正常解码
- 唯一文件哈希：36/36
- 生成方式：公版历史扫描的非生成式图像处理，不使用 AI 重绘

## 代码检查

- `lib/card-decks.ts` 已指向 `/tarot/lenormand/dondorf-v103/`
- 旧 `/tarot/lenormand/dondorf/` 目录从发布包移除，避免误引用
- `scripts/fetch-public-card-assets.py` 验证新 V10.3 目录中的 36 张牌
- 内容版本：14
- V10.2 默认图源说明可迁移到新说明；自定义文案不覆盖
- V10.2 的旧组件自动清理逻辑继续保留

## CSS 检查

V10.3 的覆盖规则位于 `app/public.css` 文件末尾，优先级高于旧牌背样式：

- RWS：纯 `#74627f` + `☆`
- Lenormand：纯 `#d8cde4` + `○`
- 两套均保留细内边框
- 牌组选择缩略图同步
- 正面牌图不增加文字覆盖层

## 构建说明

当前执行环境缺少项目 `node_modules`，因此裸 `tsc` 会因 Next/React/Node 类型包不存在而产生依赖解析错误；这不是 V10.3 源码类型错误的有效判据。V10.2 已在用户真实 VPS 上完成 Next.js production build。V10.3 的 TypeScript 变更仅为字符串、版本号和静态路径，本地另行执行 TypeScript `transpileModule` 语法检查与资源完整性检查。

最终 production `next build --webpack` 仍由 VPS 的 Docker build 执行；失败时更新脚本不会进入容器替换成功状态。
