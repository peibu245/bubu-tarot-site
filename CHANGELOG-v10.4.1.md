# 不不tarot V10.4.1 — 用户 CSS 合并版

本补丁以 V10.4 为基础，仅合并用户提供的 `public.css` 调整，不改后台数据、价格、联系方式、卡牌内容或 API。

## 合并内容
- 抽牌舞台桌面高度提高为 `clamp(540px,47vw,660px)`，最终最小高度提高到 `580px`。
- 桌面塔罗牌背尺寸调整为 `110×186px`，雷诺曼为 `102×168px`。
- 手机抽牌舞台提高到 `min(70dvh,590px)` / `min-height:520px`。
- 手机塔罗牌背调整为 `68×116px`，雷诺曼为 `64×104px`。
- 保留 V10.4 的极简牌背样式。

## 兼容补丁
`DrawStudio.tsx` 实际渲染桌面 11 个、手机 7 个抽牌点击区；用户 CSS 中原规则仍为 13/9 列。为避免牌扇右侧出现无点击区域，本版只额外将 `.fan-hit-grid` 对齐为桌面 11 列、手机 7 列。

抽牌组件当前已把 `--from-rot` 固定为 `0deg`，因此用户 CSS 中删除旧的末尾动画覆盖不会重新引入“抽出的牌歪着飞”的问题。
