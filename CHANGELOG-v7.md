# 不不tarot V7 — 实时网页编辑器 + 历史回滚 + 模块化页面

## 这次最重要的变化

### 1. 真正的实时草稿预览
- 后台“页面文案工作台”升级为左右分栏：左边编辑，右边嵌入真实公开页面。
- 标题、正文、按钮等已登记文案在输入时立即覆盖到右侧预览，不需要先保存。
- 右侧可编辑文字会出现轻量虚线提示。
- 点击右侧某段可识别文字，会自动切回“逐项编辑”并滚动到左侧对应字段。
- 字号比例、字间距、行距也会同步到实时草稿预览。
- 预览模式会阻止普通链接跳走，避免编辑时误导航。

> 当前实时覆盖主要针对 `pageText` 文案和排版。价格、活动、联系方式以及新建模块仍以“保存并发布”后的正式页面为最终结果。

### 2. 历史版本与一键回滚
- 每次“保存并发布”之前都会自动保留上一版。
- 后台新增“历史版本与回滚”，最多显示最近 30 版。
- 可以直接恢复任意历史版本。
- 恢复旧版之前，当前版本也会先自动备份，所以恢复后仍可反悔。
- VPS 直接复用 `/data/backups`；不额外引入新的数据库依赖。
- Cloudflare / D1 版本增加 `site_content_history` 表并自动维护最近 30 版。

### 3. 从“自由内容块”升级为模块化页面搭建器
原来的 Markdown / 安全 HTML 内容块自动迁移为“富文本模块”，同时新增：

- 富文本（Markdown / 安全 HTML）
- 标题区（小标签 + 大标题 + 说明）
- 提示框
- 按钮 / 链接
- 图片 + 图注
- 双栏文字
- FAQ 折叠问答
- 分隔线

模块支持：
- 主视觉之后 / 页脚之前两个位置；
- 显示 / 隐藏；
- 拖动排序；
- ↑ ↓ 按钮排序（手机或不方便拖动时也可用）；
- 按页面独立管理。

### 4. 安全边界继续保留
- 富文本仍不会执行任意 JavaScript。
- HTML 继续过滤 `<script>`、iframe、事件属性等危险内容。
- 按钮链接只接受 `https://`、站内 `/...`、`#...` 等安全形式。
- 图片只接受 HTTPS 或站内路径。

## 数据兼容
内容版本从 V8 升级为 V9。

读取 V6 / V8 保存内容时：
- 已改过的页面标题和正文保留；
- 原价格、活动、联系方式、政策保留；
- 旧 `richBlocks` 自动补上 `kind: "richText"` 以及新模块所需字段；
- 不会因为升级恢复默认 AI 文案。

实际迁移测试已验证：旧自定义首页标题保留、价格保留、旧自由内容块成功迁移为富文本模块。

## VPS 更新
覆盖服务器上的项目代码后执行：

```bash
chmod +x vps/update-existing-server.sh
./vps/update-existing-server.sh
```

脚本会先把当前 `vps/data/site-content.json` 复制到 `vps/data/backups/site-content-before-v7-时间戳.json`，再重新构建容器。

## 本地验证结果
- 47 个 TS / TSX 文件均通过 TypeScript `transpileModule` 语法检查，0 个语法错误。
- 9 个文案页面共 284 个字段，284 个字段全部能在默认内容中找到对应键。
- VPS 文件存储路径完成真实读写测试：保存 → 产生历史版本 → 恢复旧版，结果正确。
- V6 旧 `richBlocks` 迁移测试通过。
- 当前环境无法从 npm registry 补齐缺失依赖，因此没有完成正式 `next build`；VPS 的 Docker 构建会执行最终 Next.js 编译。
