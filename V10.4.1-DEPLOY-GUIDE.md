# V10.4.1 FinalShell 部署

目标文件：`bubu-tarot-vps-v10.4.1-user-css.tar.gz`

1. 上传到 `/root/` 并校验 SHA-256。
2. 备份当前网站：
   `tar -C /root -czf "/root/bubu-tarot-site-before-v10.4.1-$(date +%Y%m%d_%H%M%S).tar.gz" bubu-tarot-site`
3. 确认 `/root/bubu-tarot-site/vps/.env` 与 `/root/bubu-tarot-site/vps/data` 存在。
4. 覆盖：
   `tar -xzf /root/bubu-tarot-vps-v10.4.1-user-css.tar.gz -C /root/bubu-tarot-site`
5. 执行：
   `cd /root/bubu-tarot-site && chmod +x vps/update-existing-server.sh && ./vps/update-existing-server.sh`
6. 最终出现 `Update complete.` 才视为成功。

上线后重点看 `/notes`：桌面/手机牌扇尺寸、舞台高度、左右点击区域、抽牌归正动画。
