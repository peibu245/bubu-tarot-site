# 不不tarot V9 — 抽一张看看

## 本版重点

V9 重做 `/notes` 的抽牌体验，把原来“点牌后去旁边/下方看结果”的结构改成真正围绕牌桌完成的交互。

### 1. 多牌堆入口

当前可用：

- **韦特–史密斯**：78 张，使用 Wikimedia Commons 的 Pam-A 历史扫描。
- **雷诺曼**：36 张，使用 1799 年 Johann Kaspar Hechtel《Das Spiel der Hofnung / The Game of Hope》历史图像。
- **知识牌堆**：78 张动画牌，翻开后直接在牌面显示后台维护的小科普内容。

已经预留但暂不开放：

- **马赛**：等确定一套完整、来源清楚且画面统一的历史公版后再开放。
- **托特**：不在本版直接使用 Frieda Harris 的实际牌面，避免把尚未厘清的现代权利问题塞进商业站点。

### 2. 抽牌结果直接出现在牌上

- 点击牌扇里的任意位置，所选牌会从原位置抽出。
- 牌移动到牌桌中央并翻面。
- 韦特–史密斯显示真实历史牌面与中英文牌名。
- 雷诺曼显示历史牌面与编号/牌名。
- 知识牌堆把标签、标题和正文直接排在卡片正面。
- 删除旧版“抽牌在中间、结果另放右侧/下方”的注意力分裂。

### 3. 真正的洗牌动画

点击「洗牌」后：

1. 摊开的牌收拢到中央；
2. 牌群顺时针散开并交错旋转；
3. 再次聚成牌堆；
4. 重新摊成牌扇。

洗牌不仅是动画：每次都会重新随机化物理位置与实际卡牌的映射。

系统开启“减少动态效果”时，动画会自动简化。

### 4. 桌面与手机分别适配

- 桌面端使用长条牌桌，让牌扇横向铺满可交互区域。
- 手机端保留完整牌组逻辑，但以更紧凑的牌扇显示，避免页面被 78 张牌撑成长列表。
- 抽出的牌始终留在牌桌中央，不要求用户再向右或向下寻找结果。

### 5. 图像来源与部署

为避免把几十 MB 的历史扫描直接塞进更新包，V9 增加：

`./scripts/fetch-public-card-assets.py`

服务器执行更新时，会在 Docker build **之前**检查并下载：

- 78 张 500px Waite–Smith 公版缩略图；
- 1 张 960px Game of Hope 36 卡历史图表。

已存在且通过基本图片校验的文件不会重复下载。下载失败时脚本会直接停止，**不会重新构建或重启当前正在运行的网站**。

详细来源见 `CARD-IMAGE-SOURCES.md`。

### 6. CMS 与旧数据迁移

- 内容版本升级到 V11。
- `/notes` 在后台名称改为「抽一张」。
- 新增牌堆名称、说明、洗牌文案、抽牌提示、图源说明等可编辑字段。
- 旧版用户自己修改过的标题和正文不会被强行覆盖。
- 只有仍保持旧默认值的“小科普”相关默认文案会迁移为「抽一张看看」。

### 7. V8.1 手机联系方式修复已合并

V9 已包含 V8 联系方式中心及 V8.1 手机端修复：手机联系弹窗更紧凑、安全区内关闭按钮，以及返回手势优先收起二维码/关闭弹窗；桌面端保持原样。
